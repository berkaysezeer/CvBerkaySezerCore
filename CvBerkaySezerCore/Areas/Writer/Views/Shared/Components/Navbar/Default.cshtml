@using EntityLayer.Concrete
@model List<Announcement>
<nav class="navbar col-lg-12 col-12 p-0 fixed-top d-flex flex-row">
    <div class="text-center navbar-brand-wrapper d-flex align-items-center justify-content-center">
        <a class="navbar-brand brand-logo mr-5" href="/Writer/"><img src="~/template/writer/images/logo.svg" class="mr-2" alt="logo" /></a>
        <a class="navbar-brand brand-logo-mini" href="/Writer/"><img src="~/template/writer/images/logo-mini.svg" alt="logo" /></a>
    </div>
    <div class="navbar-menu-wrapper d-flex align-items-center justify-content-end">
        <button class="navbar-toggler navbar-toggler align-self-center" type="button" data-toggle="minimize">
            <span class="icon-menu"></span>
        </button>
        <ul class="navbar-nav navbar-nav-right">
            <li class="nav-item dropdown">
                <a class="nav-link @(Model.Count > 0 ? "count-indicator" : "") dropdown-toggle" id="notificationDropdown" href="#" data-toggle="dropdown">
                    <i class="icon-bell mx-0"></i>
                    <span class="count"></span>
                </a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
                    <p class="mb-0 font-weight-normal float-left dropdown-header">Duyurular</p>

                    @foreach (var ann in Model)
                    {
                        <a class="dropdown-item preview-item" href="/Writer/Announcement/Detail/@ann.Id">
                            <div class="preview-item-content">
                                <h6 class="preview-subject font-weight-normal">@ann.Title</h6>
                                <p class="font-weight-light small-text mb-0 text-muted">
                                    @(ann.Content.Length > 20 ? ann.Content.Substring(0, 17) + "..." : ann.Content) <span class="badge badge-primary">@ann.Date.ToShortDateString()</span>
                                </p>
                            </div>
                        </a>

                        <hr />
                    }
                </div>
            </li>
            <li class="nav-item nav-profile dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" id="profileDropdown">
                    <img src="@ViewBag.ImageUrl" alt="profile" />
                </a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="profileDropdown">
                    <a class="dropdown-item" href="/Writer/Profile">
                        <i class="ti-settings text-primary"></i>
                        Profil
                    </a>
                    <a class="dropdown-item" href="/Writer/Login/LogOut">
                        <i class="ti-power-off text-primary"></i>
                        Çıkış
                    </a>
                </div>
            </li>
        </ul>
        <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center" type="button" data-toggle="offcanvas">
            <span class="icon-menu"></span>
        </button>
    </div>
</nav>